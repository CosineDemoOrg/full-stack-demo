name: tableau_workflow
version: 1.0.0
config-version: 2

profile: tableau_profile

model-paths: ["models"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]

models:
  tableau_workflow:
    +materialized: table
    +schema: analytics